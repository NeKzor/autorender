{
    "tasks": {
        "start": "deno run --allow-env --allow-read=.env,.env.defaults,.env.example,log/client --allow-write=log/client --allow-run=/bin/bash --allow-net=autorender.portal2.local:8001 main.ts",
        "start:dev": "deno run --allow-env --allow-read=.env,.env.defaults,.env.example,log/client --allow-write=log/client --allow-run=/bin/bash --allow-net=autorender.portal2.local:8001 main.ts --watch",
        "test": "deno test --allow-net --allow-read --allow-env"
    },
    "fmt": {
        "options": {
            "useTabs": false,
            "lineWidth": 120,
            "indentWidth": 4,
            "singleQuote": true,
            "proseWrap": "preserve"
        }
    }
}
